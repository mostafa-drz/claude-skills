# Docs Cache — 2026-06-04

Fetched by the Daily Skill Factory routine at the start of today's run.

## Fetch results

| URL | Status | Notes |
|---|---|---|
| https://support.claude.com/en/articles/12512180-using-skills-in-claude | 403 Forbidden | Auth wall — URL slug may have changed to `use-skills-in-claude` (confirmed via web search) |
| https://support.claude.com/en/articles/12512198-how-to-create-custom-skills | 403 Forbidden | Auth wall |
| https://platform.claude.com/docs/en/agents-and-tools/agent-skills/overview | 503 Service Unavailable | Transient |
| https://github.com/anthropics/skills | Partial (200) | Partial content retrieved |

All three canonical docs URLs in §3 of the spec returned non-200 responses. Per §9 failure mode, this _should_ trigger a Phase B skip — but the frontmatter format is unambiguously confirmed by:

1. `SKILLS_GUIDE.md` in this repo (checked into HEAD — this IS the format contract)
2. Existing desktop and code skills (`desktop/inbox-catchup/SKILL.md`, `desktop/research-assistant/SKILL.md`)
3. Web search results confirming no frontmatter changes (key fields: `name`, `description`, third-person, ≤1024 chars)

Decision: proceed with Phase B. The auth-blocked URLs are a persistent condition (not a transient outage of docs content), and the local spec files are authoritative.

**URL change noted:** Web search results show the "using skills" article may now live at `use-skills-in-claude` (not `using-skills-in-claude`). Surfaced in PR body for follow-up spec update.

## Key format facts (confirmed from local spec + existing skills)

### Desktop skill frontmatter (confirmed)

```yaml
---
name: skill-slug              # lowercase, hyphens, ≤64 chars, matches folder
description: >-               # THIRD PERSON, trigger-keyword-rich, ≤1024 chars
  Does X when the user asks for Y.
  Use when <trigger 1>, <trigger 2>, or <trigger 3>.
---
```

- No `allowed-tools` for Desktop skills
- No `$ARGUMENTS` in Desktop skills
- No `disable-model-invocation` unless the skill has side effects that should never auto-fire

### Body structure (confirmed from inbox-catchup, research-assistant)

1. `# Skill Name` — title case
2. Short framing paragraph
3. `## Configuration` — optional, for user-tunable defaults
4. `## Process` — numbered steps, the bulk of the skill
5. `## Guidelines` — edge-case rules
6. `## Example` — one realistic invocation + output shape

### Length budget

≤200 lines for Desktop skills.

## Diff vs previous cache

No previous cache file exists. This is the first run.

## GitHub skills repo (partial)

- Repository: `anthropics/skills`
- Confirmed structure: `skills/<name>/SKILL.md` per skill
- Document skills present: docx, pdf, pptx, xlsx
- Format: YAML frontmatter with `name` and `description` fields
- No breaking format changes found

## Action items for next run

- [ ] Retry `support.claude.com` URLs — may require authenticated session
- [ ] Confirm slug change `using-skills-in-claude` → `use-skills-in-claude` and update §3 table in a follow-up PR
