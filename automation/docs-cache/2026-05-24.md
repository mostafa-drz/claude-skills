# Docs Cache — 2026-05-24

Fetched at the start of today's Daily Skill Factory run.

---

## Source 1 — Using Skills in Claude (end-user)

**URL**: https://support.claude.com/en/articles/12512180-using-skills-in-claude  
**Fetch status**: 403 Forbidden (auth-gated in this environment)  
**Web search supplemented**: Yes — canonical URL appears to have moved slightly to `use-skills-in-claude` (slug change noted; table update tracked in PR body)

**Key content from web search**:
- Skills extend Claude's capabilities with specialized knowledge and workflows.
- Available on Free, Pro, Max, Team, and Enterprise plans (requires code execution enabled).
- Invocation: type `/` in the sidebar to see available skills, or describe the task naturally — Claude activates the matching skill automatically.
- Individual members can toggle example skills, access provisioned skills, and upload personal skills in Customize > Skills.
- Skills load on-demand: only the description (~100 tokens) is always in context; the full SKILL.md body loads only when triggered.

---

## Source 2 — How to Create Custom Skills

**URL**: https://support.claude.com/en/articles/12512198-how-to-create-custom-skills  
**Fetch status**: 403 Forbidden (auth-gated in this environment)  
**Web search supplemented**: Partial — key authoring rules confirmed via platform docs

**Key content from platform docs + web search**:
- Each skill is a directory with a `SKILL.md` file containing YAML frontmatter.
- Required frontmatter: `name` (≤64 chars, lowercase + hyphens) and `description` (≤1024 chars, non-empty).
- Description should say *what* the skill does and *when* Claude should use it (trigger keywords).
- claude.ai: upload as a zip file via Settings > Features. Personal, not org-shared.
- Claude Code: filesystem-based at `~/.claude/skills/` or `.claude/skills/`. No upload needed.

---

## Source 3 — Agent Skills Overview (Platform/API)

**URL**: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/overview  
**Fetch status**: 200 OK  
**Fetched**: 2026-05-24

**Key content**:

### Three levels of progressive loading
| Level | When Loaded | Token Cost | Content |
|-------|------------|------------|---------|
| Level 1: Metadata | Always (at startup) | ~100 tokens per skill | `name` + `description` from frontmatter |
| Level 2: Instructions | When triggered | Under 5k tokens | SKILL.md body |
| Level 3+: Resources | As needed | Effectively unlimited | Bundled files via bash |

### Skill structure
```
skill-folder/
├── SKILL.md        (main instructions — required)
├── FORMS.md        (optional supplementary docs)
└── scripts/        (optional executable scripts)
    └── script.py
```

### Frontmatter requirements
- `name`: ≤64 chars, lowercase + hyphens only, no reserved words ("anthropic", "claude")
- `description`: ≤1024 chars, non-empty, no XML tags, include *what it does* and *when to use it*

### Where Skills work
- **claude.ai**: Pre-built + custom skills. Custom = individual user only; not org-wide.
- **Claude Code**: Custom skills only. Filesystem-based, no upload needed.
- **Claude API**: Both. Workspace-wide sharing.

### Runtime constraints relevant to Desktop skills
- claude.ai network access: varies by user/admin settings (can be full, partial, or none)
- Claude Code: full network access

### Pre-built skills available
- PowerPoint (pptx), Excel (xlsx), Word (docx), PDF (pdf)

### Open-source skills in anthropics/skills repo
- Claude API skill (API reference + SDK docs for 8 languages), bundled with Claude Code

---

## Diff vs previous cache

No previous cache file exists — this is the first run. No diff available.

---

## Upstream changes to note

- `support.claude.com` URLs returned 403 in this environment (likely auth-gated). Web search found that at least one URL slug changed from `using-skills-in-claude` to `use-skills-in-claude`. A follow-up PR should update the spec table.
- Platform docs content is substantively unchanged from what was used to author existing skills (frontmatter schema, progressive loading, 3-level architecture all match prior SKILLS_GUIDE.md conventions).
