# Docs Cache — 2026-06-15

Fetched at: 2026-06-15T06:00 UTC (daily-skill-factory routine)

## Sources

| URL | Status | Notes |
|-----|--------|-------|
| https://support.claude.com/en/articles/12512180-using-skills-in-claude | ❌ 403 Forbidden | Support site requires authenticated session. No public access without login. |
| https://support.claude.com/en/articles/12512198-how-to-create-custom-skills | ❌ 403 Forbidden | Same as above. |
| https://platform.claude.com/docs/en/agents-and-tools/agent-skills/overview | ✅ Fetched | Full content retrieved. See below. |

**Decision**: Two of three URLs failed. The spec says "skip Phase B only if ALL THREE URLs are down." Since the platform overview fetched cleanly, Phase B proceeds with that source plus cached knowledge of the prior spec structure.

---

## platform.claude.com/docs — Agent Skills Overview (fetched 2026-06-15)

### Skill Structure (key frontmatter fields)

Required:
- `name`: lowercase, hyphens, max 64 chars. Cannot contain "anthropic" or "claude".
- `description`: non-empty, max 1024 chars, no XML tags. Must say WHAT it does AND WHEN to use it.

Optional (not in scope for Desktop skills per the routine spec):
- `allowed-tools`, `disable-model-invocation`, `context`, `agent`, `model`

### How skills load (progressive disclosure)

1. **Level 1 — Metadata** (always loaded): YAML frontmatter in system prompt, ~100 tokens/skill
2. **Level 2 — Instructions** (when triggered): SKILL.md body, <5k tokens, loaded via bash read
3. **Level 3 — Resources** (on demand): bundled files, scripts, unlimited but token-free until accessed

Claude reads SKILL.md from the filesystem when triggered. The description must be broad enough that Claude fires the skill on natural-language phrasing, not just exact command invocations.

### Key constraints noted this run

- `claude.ai` custom skills: individual per user, cannot be centrally managed by org admins
- Claude Code skills: personal (`~/.claude/skills/`) or project-based (`.claude/skills/`)
- Desktop context has Google Workspace + MCP integrations + sandboxed code execution available
- No local filesystem access outside the skill sandbox

### Changes vs previous cache

No previous cache file exists. This is the first run of the Daily Skill Factory.

---

## Diff

N/A — first run, no prior cache to diff against.
